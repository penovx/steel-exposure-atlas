(()=>{'use strict';function close(except=null){document.querySelectorAll('.info-hint.is-open').forEach(wrap=>{if(wrap===except)return;wrap.classList.remove('is-open');const button=wrap.querySelector('.info-hint-button');const pop=wrap.querySelector('.info-hint-popover');if(button)button.setAttribute('aria-expanded','false');if(pop)pop.hidden=true})}function init(){document.querySelectorAll('.info-hint').forEach(wrap=>{const button=wrap.querySelector('.info-hint-button');const pop=wrap.querySelector('.info-hint-popover');if(!button||!pop)return;button.addEventListener('click',e=>{e.stopPropagation();const opening=!wrap.classList.contains('is-open');close(wrap);wrap.classList.toggle('is-open',opening);button.setAttribute('aria-expanded',String(opening));pop.hidden=!opening})});document.addEventListener('click',e=>{if(!e.target.closest('.info-hint'))close()});document.addEventListener('keydown',e=>{if(e.key==='Escape')close()})}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();})();